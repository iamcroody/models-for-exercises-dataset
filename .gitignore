.venv/
__pycache__/
*.pyc
.DS_Store
.ipynb_checkpoints/

# Derived from the pinned submodule by a seeded script — regenerate, don't commit
data/processed/

# Training checkpoints and optimizer state — large and disposable.
outputs/

# Trained adapters. The main run is committed (34 MB, fp16) so the reported
# numbers can be reproduced without a GPU. Ablation adapters are not: their
# results are in reports/ and 04_train_lora.py regenerates them.
models/*
!models/r16-all-linear/

# Editor state (do not publish)
.claude/
.vscode/
