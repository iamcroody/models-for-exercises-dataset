[project]
name = "models-for-exercises-dataset"
version = "0.1.0"
description = "Fine-tuning experiments on the exercises dataset"
requires-python = ">=3.11"
dependencies = [
    "accelerate>=1.14.0",
    "datasets>=5.0.1",
    "peft>=0.20.0",
    "scikit-learn>=1.9.0",
    "torch>=2.13.0",
    "transformers>=5.14.1",
    "trl>=1.9.2",
]
