[project]
name = "models-for-exercises-dataset"
version = "0.1.0"
description = "LoRA fine-tuning of a decoder model that adapts real gym exercises to household objects"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.13.0",
    "transformers>=5.14.0",
    "trl>=1.9.0",
    "peft>=0.20.0",
    "accelerate>=1.14.0",
    "datasets>=5.0.0",
    "rouge-score>=0.1.2",
    "sentence-transformers>=3.0.0",
]
